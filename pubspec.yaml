name: gold_calc_shagraf
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  
  # --- تغییر انجام شده: ارتقا نسخه intl ---
  intl: ^0.20.2 
  http: ^1.2.0
  html: ^0.15.4
  home_widget: ^0.6.0
  tapsell_plus: ^2.3.2
  workmanager: ^0.6.0
  
  # اگر قبلاً این را اضافه کردید نگه دارید، اگر نه نیازی نیست
  # flutter_launcher_icons: ^0.13.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  # تنظیمات فونت
  fonts:
    - family: Vazirmatn
      fonts:
        - asset: assets/fonts/Vazirmatn.ttf